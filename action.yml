name: 'Journal page generator'
description: 'Generate new journal file every week day'
author: 'Sound1ab'
inputs:
  GH_TOKEN: # change this
    required: true
    description: 'GitHub access token'
    default: ${{ github.token }}
runs:
  using: 'node12'
  main: 'dist/index.js'
